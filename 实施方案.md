# vdriver 实施方案(Ascend CANN 虚拟驱动 / 假 libascend_hal)

> 依据:4 份分层调研(归档于 `docs/`)+ 上层《meta_device_mock选型调研报告.md》。
> 所有结论均带 文件:行号 证据;未确认项在 §8 单列。

---

## 1. 目标与非目标

**目标**:提供一个假 `libascend_hal.so`,使 **torch_npu → libopapi(libnnopbase)→ libruntime 的全部上层真实流程**离设备走通(dispatch、两段式 aclnn、tiling/选核、二进制加载、任务组包、SQE 下发、同步回收、D2H 回读),上层零源码改动。

**非目标**:不保证算子计算结果正确;不做故障注入/HCCL 多卡/profiling 全量;不改 driver 仓内核态代码。

## 2. 关键决策表

| # | 决策点 | 选择 | 理由(证据见 docs/) |
|---|---|---|---|
| D1 | 实现哪个接口 | **hal\***(`npu_driver_base.hpp:15-233` 约 160 个 weak 符号直链) | 无需 `CFG_DEV_PLATFORM_PC` 重编 runtime;cmodel_driver 走的 drv\* 静链形态需改构建,弃 |
| D2 | 集成方式 | **同名同 SONAME `libascend_hal.so`**,LD_LIBRARY_PATH 前置或替换 `lib/stub/` 下官方桩 | DT_NEEDED 按 soname 查找(实测);零源码改动;msprof/plog/atrace/awatchdog/slog 的 dlsym 同文件命中 |
| D3 | 库构成 | **h2c.py 全量桩(~361 符号)为基座 + 强语义模块覆盖,单一 .so** | 消除"直链走 vdriver、dlsym 走官方桩"的分裂 |
| D4 | RUN_MACH / 地址模式 | **`RUN_MACHINE_VIRTUAL`(1)+ ADDR_MODE 非 0(flat)** | 命中 `RUNTIME_WHEN_NO_VIRTUAL_MODEL_RETURN`(`npu_driver_base.hpp:271`)短路 halHostRegister 两条路径(`npu_driver_mem.cc:288/302`);cqeDepth 强制 256(`npu_driver_res.cc:1562-1565`)。PHYSICAL 等价可行,联调异常时的 fallback |
| D5 | SoC 型号 | **Ascend910B1**(经 `halGetSocVersion` 或 `INFO_TYPE_VERSION`+chipType 推导) | SoC 名必须有对应 ini(`data/platform_config/cann/Ascend910B1.ini`)否则 tiling 在 `InitRuntimePlatformInfos` 报错;型号来源是 runtime 编译期注册表+hal 兜底(`runtime.cc:655-731`),vdriver 只需自洽 |
| D6 | 任务完成模型 | **同步完成**:`halSqTaskSend` 内解释执行 SQE 并立即推进 head;`halCqReportGet` 恒 `count=0`,不产 CQE | 任务回收走 `halSqCqQuery` SQ_HEAD 前进 + `FinishedTaskReclaim`(`stars_engine.cc:395/709`),完全避开 CQE 布局/phase 翻转坑;无需定时线程 |
| D7 | AICPU 策略 | `halGetDeviceInfo(MODULE_TYPE_AICPU)` 返 0 核 + `halSupportFeature` 不报 AICPUSD_LAZY_START | AICPU 算子 fail-fast 报 `RT_ERROR_FEATURE_NOT_SUPPORT`(`api_impl.cc:7239-7247`),纯 AI Core 链路不受影响,免 tsd 桩 |
| D8 | 多进程 | 句柄表每进程一份、进程内加锁、`pthread_atfork` 重初始化(参 `awatchdog_core.c:63-70`) | 全局态不跨进程;跨进程接口(halShrId\*/halMemShare\*)回 `DRV_ERROR_NOT_SUPPORT` 走降级(`npu_driver.cc:80-84`) |

## 3. 符号契约(vdriver 实现清单)

### 3.1 强语义符号(必须真实现,17 个)

| 符号 | 语义要求 | 关键证据 |
|---|---|---|
| `halGetDeviceInfo` | ENV/VERSION/CORE_NUM/FREQUE_LEVEL(infoType=24 返 0)/ADDR_MODE/RUN_MACH 按决策表;**AICPU 核数=0** | `base.h:1177`;`npu_driver.cc:60-90/488`;`runtime.cc:4860`;torch_npu `LibAscendHal.cpp:13-15` 直查 FREQ_CONFIG |
| `halGetAPIVersion` | 返回 0。**该符号在 `npu_driver_queue.cc:685/1139` 无判空调用,缺席必崩** | 同左 |
| `halDeviceOpen` / `halDeviceClose` | 打开时初始化句柄记账;关闭清账 | `npu_driver.cc:425-450/347/373`;销毁路径必经 |
| `halGetChipCapability` | 全零 `halCapabilityInfo`(`base.h:670`)+ret 0 | `npu_driver.cc:1292` |
| `halGetSocVersion` | 返回 Ascend910B1 的 DRV SoC 版本(缺席则走 VERSION 推导兜底) | `api_c_soc.cc:29`;`runtime.cc:655-731` |
| `halSqCqAllocate` / `halSqCqFree` | sqId/cqId 唯一记账/回收;sqeSize=12、深度按 runtime 传入 | `trs_pkg.h:45`;`npu_driver_res.cc:1475/1576/1655/1705;1502/1619/1727/1851` |
| `halSqCqQuery` | **SQ_HEAD=已发 SQE 数(tail)、SQ_STATUS≠0**。纯桩 0 会使同步永久自旋 | `npu_driver_res.cc:780-856`←`stream.cc:1511`/`stars_engine.cc:357/484/766/960/1105` |
| `halSqCqConfig` | 桩 0(SQ_HEAD/SQ_PAUSE 语义) | `npu_driver_res.cc:830/856`(判空可缺席) |
| `halSqTaskSend` | 记账已发 SQE;`pos` 出参填首 SQE 位置;**解释执行 COPY/MEMSET 类 SQE**(§4.3) | `trs_pkg.h:153`;`npu_driver_res.cc:2004/2069`;拷贝类 SQE 源 `memcpy_stars.cc:157/164` |
| `halMemAlloc` / `halMemFree` | **posix_memalign 512B host 后备**(设备 flag)与 host flag 分支都真分配 | `npu_driver_mem.cc:793/805/863/946/1001/1068/1181;819/889`;torch_npu HostMalloc 也落此 |
| `halMemcpy` | **真 memmove**,方向按 `drvMemcpyKind_t`;D2H 才有真数据 | `base.h:2049`;`npu_driver_mem.cc:2203-2208`;算子 .o 经此落"设备"内存(`runtime.cc:4404/4429-4443`) |
| `halHostRegister` / `halHostUnregister` | no-op 成功(VIRTUAL+flat 下基本不命中,保底) | `npu_driver_mem.cc:200/290` |
| `halCqReportIrqWait` | 立即返 0(配合 count=0 回收路径) | `npu_driver_res.cc:2096/2185/2258/2327` |
| `halCqReportGet` | **恒 count=0 / reportPtr=null**(D6 最小路径) | `npu_driver_res.cc:2207/2277/2343` |
| `halReportRelease` | 桩 0 | `npu_driver_res.cc:2303/2367` |
| `drvGetDevNum` / `drvGetDevIDs` / `drvGetPlatformInfo` | 设备数 1 / ID 表 / 平台串(drvGetPlatformInfo 返 host 侧可禁用看门狗) | `driver.cc:76/89`;`npu_driver.cc:114`;`awatchdog_core.c:81-102` |
| `drvMemSmmuQuery` | *SSID=0 | `npu_driver.cc:457`(DeviceOpen 内) |

### 3.2 桩即可(h2c 基座覆盖)
`halGetChipFromDevice`、`halGetChipInfo`(仅 310P 细分)、`halMapErrorCode`、`halEschedCreateGrpEx`、`halSupportFeature`(建议返 false→legacy 降级)、`halGetCapabilityGroupInfo`,以及**全部其余 ~340 个导出符号**——用于满足 dlsym 消费者:msprof(`platform.cpp:450-467`)、plog 16 符号(`plog_driver_api.c:21-38`)、atrace、awatchdog、slog(`dlog_attr.c:83`)。

### 3.3 保持缺席(禁止空桩)
- `halMemcpySumbit` / `halMemcpyWait`:空桩 return 0 会**丢数据**;缺席时 H2D 管理器自动退化 `COPY_POLICY_SYNC`(`h2d_copy_mgr.cc:109-112`)。
- `halMemcpy2D` / `halMemcpyBatch` / `halGetDeviceInfoByBuff` / `halHostRegisterCapabilities`:有 null 守卫,缺省安全(`npu_driver_mem.cc:2380/2401/2203/268`,`npu_driver_res.cc:148`)。

## 4. 模块设计

### 4.1 模块划分
```
src/
├── stub_base.c    # h2c.py 生成全量桩(基座,~361 符号 return 0)
├── vdriver.c      # 全局态:句柄表/锁/atfork/初始化守卫
├── device.c       # §3.1 设备组(halGetDeviceInfo 按 D4/D5/D7 决策作答)
├── memory.c       # halMemAlloc/Free/Memcpy/HostRegister(host 后备内存账本)
├── sqcq.c         # halSqCqAllocate/Free/Query/Config/SqTaskSend(记账+head 推进)
└── sqe_interp.c   # SQE 解释器:按 task 类型分流(§4.3)
```

### 4.2 内存模型
复用 cmodel_driver 账本思路(`driver_mem.c:114-217`:512B 对齐、首次适配、切分合并)但**每段分配绑定 host 后备块**(`posix_memalign`),`halMemcpy` 对设备地址段直接 `memmove` 后备块——解决 cmodel ESL 版"账本无后备、总线不真拷"的两个坑(`driver_mem.c:305-344`、shim `busDirectRead/Write` 只校验不拷贝)。

### 4.3 SQE 解释器(vdriver 唯一的"智能"部分)
`halSqTaskSend` 收到 `halTaskSendInfo{sqId, sqe_addr, sqe_num}`(`trs_pkg.h:153`)后,按 SQE 内 task 类型分流:
- **KERNEL 类**(AI Core kernel):忽略(执行结果不 mock);
- **COPY_ONLY 类**(异步 H2D/D2H/D2D):按 SQE 内 src/dst/length 做 host `memcpy`(两端都是本进程 host 后备块);异步 D2H 读动态 shape 结果依赖此步;
- **MEMSET 类**(workspace 清零):按 value/length `memset`;
- 推进该 sqId 的 head 计数(供 `halSqCqQuery`)。
风险:SQE 二进制布局需按 `esched_topic_sqe.{c,h}` / `task_sqe` 对齐字段(偏移、长度、value 位置),L2 验收前完成逆向对照(见 §8)。

### 4.4 同步语义链
`rtSynchronizeStream` → `halSqCqQuery(SQ_STATUS/SQ_HEAD)` 轮询 → head==tail 即完成 → `FinishedTaskReclaim`(`stars_engine.cc:395/709`)。同步 `halMemcpy`/`halMemset` 走 `npu_driver_mem.cc:2203-2208` 直通,不经 SQE。异步回调流(callback CQ、`halEschedCreateGrpEx`)列入 L4,首版可回 NOT_SUPPORT 降级。

## 5. 目录结构与构建

```
vdriver/
├── 实施方案.md / README.md
├── docs/                  # 4 份调研报告归档
├── CMakeLists.txt         # 独立构建:add_library(vdriver SHARED) + SONAME=libascend_hal.so
├── stub/h2c_run.cmake     # 调 runtime/cmake/stub/h2c.py 生成 stub_base.c(输入:runtime include/driver 三个头)
├── src/                   # §4.1
├── tests/
│   ├── test_symlink.cpp   # dlopen+dlinfo 验证 soname/句柄归属(选项 c 风险的前置实验)
│   └── run_hello_cann.sh  # L1 验收脚本(LD_LIBRARY_PATH 前置 vdriver 后跑 runtime example)
└── tools/                 # SQE 字段对照笔记
```

构建要点:
- 仅依赖 runtime 仓 `include/driver/{ascend_hal.h,ascend_hal_external.h,ascend_hal_base.h}` + securec + pthread;
- `set_target_properties(vdriver PROPERTIES OUTPUT_NAME "ascend_hal" SOVERSION "")` 使 SONAME=libascend_hal.so(实测:CMake SHARED 无 VERSION 即自动 SONAME=libascend_hal.so);
- 安装建议:独立目录 `vdriver/lib/`,运行时 `export LD_LIBRARY_PATH=/path/to/vdriver/lib:$LD_LIBRARY_PATH`;不覆盖官方 `lib/stub/`(可并存、可回退);
- 包内无 RPATH(`cmake/func.cmake:386` CMAKE_SKIP_RPATH),搜索序 LD_LIBRARY_PATH 优先,前置即生效。

## 6. 分级验收

| 级别 | 内容 | 通过标准 | 预置失败点 |
|---|---|---|---|
| **L1** runtime 单体 | `runtime/example/0_quickstart/0_hello_cann`(13 步)、`2_advanced_features/kernel/0_launch_kernel` | 全程返回 0;若实现 add 语义 D2H 数值正确 | 缺 halDeviceClose/halSqCqFree → 末段 DRV_NOT_SUPPORT;halSqCqQuery 不推进 → 同步自旋 |
| **L2** opbase | 真 libopapi 跑 `aclnnAdd` 两段式(GetWorkspaceSize→aclnnAdd→Sync) | launch 不报错、同步返回;bin 加载链(binary_loader.cc:172-198 解析→halMemAlloc+halMemcpy 落库)通过 | SQE 长度/格式;MemsetAsync(workspace 清零)解释错误 |
| **L3** torch_npu | 真 torch_npu `torch.add/add_` E2E | 全流程无异常返回 | FREQ_CONFIG(infoType=24)容忍;HostAllocator 32B 对齐(`api_impl.cc:2282`) |
| **L4** 旁支 | AICPU fail-fast(应得 FEATURE_NOT_SUPPORT 而非 TSD 错);event/callback;多流 | 按决策 D7 报错;event/多流按需放开 | callback CQE 需 hostFuncCbPtr≠0(`task.hpp:92-108`,仅产 CQE 路线才涉及) |

## 7. 里程碑与工作量

| 阶段 | 内容 | 产出 | 人日 |
|---|---|---|---|
| M0 骨架 | CMake+h2c 基座+SONAME 验证(test_symlink) | 可加载空壳 .so | 1-2 |
| M1 设备+内存 | device.c/memory.c;init→SetDevice→Malloc→Memcpy 链 | L1 前半 | 2-3 |
| M2 任务通路 | sqcq.c+sqe_interp.c+同步链 | L1 全程+0_launch_kernel | 3-5 |
| M3 opbase | 真 libopapi 联调 | L2 aclnnAdd | 2-3 |
| M4 torch_npu+旁支 | L3+L4 | E2E | 3-5 |
| **合计** | | | **11-18 人日(约 2-3.5 人周)** |

(cmodel_driver 移植若走"改内存后备+删仿真器块"路线可再压缩 M1/M2 约 2-3 人日,但需从 drv\* 原型平移到 hal\* 原型,复用收益主要在内存账本与队列骨架。)

## 8. 风险与未确认项

| # | 风险/未确认 | 缓解 |
|---|---|---|
| R1 | SQE 二进制布局(偏移/字段)需逆向对照 | M2 前专项:对 `esched_topic_sqe.{c,h}`、`task_sqe`、`memcpy_stars.cc` 组包处逐一对照;用 L1 0_launch_kernel 单 SQE 环境验证 |
| R2 | cqeDepth 语义(256/1024)与 esched CQ 深度 | 按决策 D4 报 VIRTUAL+flat(强制 256);回调流深度语义 L4 再处理 |
| R3 | torch_npu 二次 dlopen 依赖、init 是否隐式查 dcmi | 未确认;L3 验收时观察,FunctionLoader 失败仅报错不崩 |
| R4 | set_env 包内 LD_LIBRARY_PATH 布局(官方包惯例) | 未确认;验收脚本显式前置 vdriver 路径规避 |
| R5 | halMemcpySumbit 误实现丢数据 | 已列入 §3.3 禁止清单 |
| R6 | 主仓 inc/ 生成物方式(tsch/model 头等)仅影响 cmodel 平移路线,不影响 hal 路线 | 独立构建只引 include/driver |
| R7 | `INFO_TYPE_VERSION` 值与 Ascend910B1 推导链自洽 | M1 用 `rtGetSocVersion`/`aclrtGetSocName` 反查验证,必须命中 `Ascend910B1.ini` |

## 9. 附录:调研报告索引(docs/)
- `01_逐符号实现详单.md` —— 符号契约/VIRTUAL 决策/SoC 来源/CQE 字段
- `02_cmodel_driver复用解剖.md` —— 复用判定表/完成回报最简实现/独立编译先例
- `03_集成安装设计.md` —— 链接事实/三种替换选项/dlsym 兼容清单/SoC 传导链
- `04_上层兼容性核对.md` —— 样例→符号映射/opbase 增量/torch_npu 触点/分级验收
