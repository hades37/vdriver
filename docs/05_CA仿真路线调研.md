# 05 · CA(CAMODEL)仿真路线调研

> 2025-09-01 · 动机:Kimi-K3 单卡用例暴露"内核不模拟执行"边界(形状数据依赖算子),
> 转向验证选型报告方案②——runtime 官方仿真形态(CA = **CAMODEL**,C 模型仿真)。

## 一、组件全貌(源码 + 本机 toolkit 双确认)

| 组件 | 位置 | 作用 |
|---|---|---|
| cmodel_driver | `runtime/src/cmodel_driver`(42 hal* + 34 drv*) | 官方主机版驱动后端,`driver_impl.c:158` 经 `startModel()` 对接仿真器 |
| runtime_cmodel_<soc> | `src/runtime/cmake/cmodel.cmake:793` | 链 `npu_drv_pvmodel_*`,产物 libruntime_cmodel.so |
| **runtime_camodel_<soc>** | 同上 :842 | 链 `npu_drv_camodel_*`,产物 **libruntime_camodel.so** |
| 构建入口 | `tests/cmodel_test/build_runtime_cmodel_product.sh --product_type=<soc>` | 一键构建(产品表含 ascend910B1) |

## 二、本机 toolkit 自带 Ascend910B1 完整仿真栈(重大发现)

`$TK/x86_64-linux/simulator/Ascend910B1/`:
- **libruntime_camodel.so**(6219 个导出符号的完整 runtime;**NEEDED 不含 libascend_hal**,
  直接链 libnpu_drv_camodel.so → libstars/libffts_model/libmodel_top/libmcu_*——芯片仿真模型本体)
- libnpu_drv_camodel.so / libnpu_drv_pvmodel.so(驱动后端,闭源)
- config.json / config_stars.json / config_hwts.json(仿真芯片配置)
- 另有全系 SoC 目录(310/910A/910B*/950PR/920A/dav_*/…)

## 三、实证结果(✅ 已跑通,数值正确,2025-09-01)

**最终配方(PV 变体,干净收尾 exit=0)**:
```
libruntime.so    → simulator/Ascend910B1/lib/libruntime_cmodel.so   (PV runtime)
libascend_hal.so → simulator/Ascend910B1/lib/libnpu_drv_pvmodel.so  (PV 驱动)
LD_LIBRARY_PATH=/tmp/ca_lib:$TK/lib64:$SIM/lib:$TK/../lib64, cwd=$SIM
```
hello_cann 结果:**exit=0,"Sample run successfully!",8/8 数值匹配**
(result 1.5/3.0/…/12.0 —— 内核 x86 真仿真执行),"ACL finalize successfully"。

过程要点(踩坑记录):
1. **符号 interposition 是最大坑**:libopapi/libnnopbase 的 DT_NEEDED
   libascend_hal.so 一旦被真包/自研 hal 满足,其 hal*/drv* 符号会抢先绑定,
   CA 驱动被整体绕过(表现为 halSqCqAllocate ret=3)。解法:libascend_hal.so
   直接软链到仿真驱动自身,单一实现,零分流;
2. runtime 源码仓的 cmodel_driver(调 startModel)与 toolkit 预编译驱动**版本
   不一致**(源码引用 tsRegDrvReportIrqTriger 等本包不存在的符号),预编译驱动
   自包含(仅 libc 未定义)——直接用预编译件;
3. 官方构建脚本 `build_runtime_cmodel_product.sh --product_type=ascend910B1`
   可完整编出源码版 runtime_cmodel/camodel(含第三方 protobuf/abseil 源码编译,
   约 20 分钟),但产物驱动与 toolkit 仿真模型库符号不兼容,当前以预编译件为准;
4. CAMODEL 变体(libruntime_camodel + libnpu_drv_camodel)同样数值正确,但
   进程收尾 double-free(官方栈内部问题),PV 变体干净——以 PV 为准。

### 历史记录(首次尝试,未通)

接入配方(LD_LIBRARY_PATH):
```
/tmp/ca_lib/libruntime.so → 符号链接到 libruntime_camodel.so   # 顶替 DT_NEEDED libruntime.so
/tmp/ca_lib/ 内含仿真器全部 so 的链接 + libascend_hal.so(vdriver 产物,仅兜底
  满足 libopapi/libnnopbase 的 DT_NEEDED——camodel runtime 自己不用它)
LD_LIBRARY_PATH=/tmp/ca_lib:$TK/lib64:simulator/Ascend910B1/lib:$TK/../lib64
```
- ✅ **ACL init successfully**(libruntime_camodel 全链加载成功)
- ❌ rtSetDevice 507033:`halSqCqAllocate` 返回 3(CA 驱动内部拒绝),StarsEngine
  构造 3 秒后析构;CAMODEL_LOG_PATH 无日志产出(闭源,内部原因待查)

## 四、与 vdriver 的定位对比

| | vdriver(假 HAL) | CA/CAMODEL(官方仿真) |
|---|---|---|
| 内核执行 | 不执行(数值非目标) | **x86 真仿真执行,数值正确** |
| 形状数据依赖算子(nonzero/topk 路由/掩码) | 全部踩坑,需 host 回退 shim | 应原生支持 |
| 维护方 | 自研(语义缺口自担) | 官方(cmodel_driver + 仿真模型随包) |
| 速度 | ~原生 | 慢 2-4 个数量级(整芯片 CPU 模拟),仅适合算子/小网络级 |
| 大模型训练(Kimi-K3) | 流程可推(待收口) | 不现实 |

## 四½、L3/L4 上 PV 仿真(2025-09-01,同日追加)
| 测试 | 结果 | 说明 |
|---|---|---|
| L3 torch_npu x+y | ✅ **数值正确** z=[3,4,5,6] | torch 全栈 → opapi → aclnnAdd → 仿真执行 |
| L4 双流/事件/同步 | ✅ 流程全过 exit=0 | 修复了测试自身的跨流竞态(vdriver 同步式 mock 曾掩盖) |
| 收尾 abort | 已知问题 | 官方栈 atexit→aclFinalize 内部 double-free;`L3_SKIP_TEARDOWN=1` 跳过(工作完成后) |
| ⚠ 官方栈缺口 | **async H2D(pinned+non_blocking)数据不落** | 同步拷贝路径全对;真仿真下竞态与该缺口叠加,已在上游层面记录 |

## 五、路线定位(更新)
- ✅ CA/PV 仿真已跑通 L1/L2/L3/L4(数值正确)——承接"算子级正确性验收层";
- 待验证:L3(torch_npu 同法切换)——torch_npu 链 libruntime.so,同一 swap;
- vdriver 保留为"大模型流程验收层"(Kimi-K3 单卡用例),双层互补;
- 双层对比价值:同一用例在 vdriver 上验"流程语义",在 PV/CA 上验"数值语义",
  交叉覆盖 mock 的固有盲区(见 vdriver/进展.md M4+ 数据通路教训)。
