# vdriver 实施进展

> 按《实施方案.md》M0–M4 里程碑推进;每阶段记录:交付物、验收结果、关键发现、code review 结论、遗留问题。
> 状态图例:✅ 完成 | 🔄 进行中 | ⬜ 未开始 | ⚠️ 有遗留

## 总览

| 阶段 | 内容 | 状态 | 完成日期 | 提交 |
|---|---|---|---|---|
| M0 | 工程骨架:h2c 桩基座 + SONAME + 加载验证 | ✅ | 2025-09-01 | 见 git log |
| M1 | 设备+内存模块(device.c/memory.c) | ✅ | 2025-09-01 | 见 git log |
| M2 | 任务通路(sqcq.c/sqe_interp.cpp + 同步链) | ✅(review 待派) | 2025-09-01 | 见 git log |
| M2 | 任务通路(sqcq.c/sqe_interp.c + 同步链) | ⬜ | — | — |
| M3 | opbase 联调(L2 aclnnAdd) | ⬜ | — | — |
| M4 | torch_npu E2E + 旁支(L3/L4) | ⬜ | — | — |

## 环境档案(重要发现)

- **CANN 真包可用**:conda env `mindspeed`(`/root/miniconda3/envs/mindspeed/`),CANN **9.1.0**(`Ascend/cann-9.1.0`,compatible ≥ C11),x86_64。
- 关键库:`x86_64-linux/lib64/{libascendcl,libruntime,libopapi}.so`;**真 HAL** 位于 `cann-9.1.0/devlib/x86_64/libascend_hal.so`(导出 305 个 T 符号)。
- 当前 shell 的 `LD_LIBRARY_PATH` 已含 CANN 路径(nnal/atb、Ascend 等)→ **dlopen 不加前置就会命中真包**,vdriver 验收脚本必须前置自身目录。
- 本机无 NPU 设备节点(纯 x86 服务器),符合 vdriver 目标场景。
- torch_npu:`import torch_npu` 当前报 "Failed to load the backend extension"(细节待 M4 排查,可能需 source set_env.sh 或处理 autoload)。

---

## M0 工程骨架 ✅(2025-09-01)

### 交付物
| 文件 | 说明 |
|---|---|
| `CMakeLists.txt` | 独立构建;RUNTIME_INC 指向 runtime 仓 include;`OUTPUT_NAME=ascend_hal` 无 VERSION → 自动 SONAME |
| `src/generated/ascend_hal_stub.c` | h2c.py 生成桩基座:378 函数(ascend_hal.h 1 + external 34 + base 343),生成命令见下 |
| `src/vdriver.c` | 版本标记 `vdriver_version()`(测试身份识别用) |
| `tests/test_m0_load.cpp` | M0 验收测试(9 检查项) |
| `.gitignore` | build/、lib/、*.so 等 |

桩再生成命令(升级 runtime 仓后执行):
```bash
python3 ../runtime/cmake/stub/h2c.py -i ../runtime/include/driver/ascend_hal.h \
  ../runtime/include/driver/ascend_hal_external.h \
  ../runtime/include/driver/ascend_hal_base.h -o src/generated/ascend_hal_stub.c
```

### 验收结果(ctest m0_load:100% 通过)
1. ✅ `dlopen("libascend_hal.so")` 成功且命中 `build/lib/libascend_hal.so`(前置 LD_LIBRARY_PATH 后)
2. ✅ SONAME=libascend_hal.so(`readelf -d` 实证)
3. ✅ 符号抽查 8/8:halGetDeviceInfo/halGetAPIVersion/halSqTaskSend/halSqCqQuery/halMemcpy/drvGetPlatformInfo/halHdcSend/halEschedCreateGrpEx
4. ✅ 桩语义:halGetAPIVersion/halGetDeviceInfo 返回 0
5. ✅ 导出统计:317 强(T) + 62 弱(W) = 379 ≈ 378 桩函数 + vdriver_version,h2c 将 weak 声明生成为弱符号
6. ✅ 对照实验:无前置时 dlopen 命中 **CANN 9.1.0 真包** devlib/x86_64/libascend_hal.so(决策 D2 的"同名抢占"两面均验证)

### 关键发现(沉淀给后续阶段)
- **glibc 启动时固化 LD_LIBRARY_PATH**:进程内 `setenv()` 对 `dlopen` 搜索**无效**(M0 实测)。所有运行脚本必须在启动前 export;ctest 用 `ENVIRONMENT` 属性注入。
- ctest `set_tests_properties` 必须在 `add_test` 之后(顺序错误只在 configure 期报错,注意)。
- 注释中写 `hal*/drv*` 之类的星号会提前终止块注释——代码注释禁用该写法。
- 真包 devlib 的 HAL 与本仓 runtime 源码版本(仓源码 vs 9.1.0)存在版本差,L1 联调时以**安装包行为为准**,必要时用真包头文件复核符号契约。

### 9.1.0 安装包链接机制核验(M0 附加,超出仓源码调研的新事实)
- `libruntime.so` 的 `DT_NEEDED = libascend_hal.so`(`readelf -d`);ldd 当前解析到真包 `cann-9.1.0/devlib/x86_64/libascend_hal.so`——**前置 vdriver 目录即完成整体替换**,与决策 D2 完全一致。
- 9.1.0 runtime 为**混合绑定**:仅 `drvGetDevNum`、`halGetDeviceInfo` 两个符号加载期直链,其余 hal* 经自有 `mmDlopen/mmDlsym` 按名取符(仓源码是全量 weak 直链,以安装包为准)。
- **符号兼容性核验通过**:libruntime/libascendcl 的全部未定义 hal/drv 符号 vs vdriver 379 个导出 = **零缺口**(comm 比对)。

### Code Review 结论(M0,代理评审 03ac9dee)
**结论:可合入,无架构阻塞。** 严重 3 项、建议 8 项、风格 4 项:
- 严重①②:测试手抄原型与真头不符(`halGetAPIVersion(int*)` 被抄成 int64_t、返回类型抄错)属 UB——**已修**:测试改 `#include "ascend_hal.h"` 用真实原型。
- 严重③:空 `LD_LIBRARY_PATH` 时 ctest ENVIRONMENT 产生尾随冒号(glibc 解释为 CWD)——**已修**:configure 期判空拼接。
- 建议④(强语义与桩同符号重复定义,直链必炸)——**已修**:改用编译期宏重命名 `-D<sym>=vdrv_stub_<sym>`(OBJECT 库在 CMake 3.22 不支持 POST_BUILD,objcopy 方案弃用)。
- 其余建议/风格(RTLD_LOCAL、realpath 前缀比较、CMAKE_DL_LIBS、CXX_STANDARD、死代码、口径注释、.gitignore)——**已采纳**。
- 评审前实测:Release/Debug 构建、SONAME、ctest、生成桩 10 符号原型逐参一致、零警告。

### 遗留
- ⚠️ torch_npu 导入失败待 M4 排查(不阻塞 M1-M3)。
- 生成桩已入库以便独立构建;升级 runtime 仓后需重新生成(命令见上)。

---

## M1 设备+内存模块 ✅(2025-09-01,review 进行中)

### 交付物
| 文件 | 说明 |
|---|---|
| `src/device.c` | halDeviceOpen/Close、halGetDeviceInfo(D4/D5/D7 口径)、halGetChipCapability、halGetSocVersion、drvGetDevNum/GetDevIDs/GetPlatformInfo(ONLINE=1)、drvMemSmmuQuery |
| `src/memory.c` | halMemAlloc/Free(host 后备+512B 对齐+头部记账+清零)、halMemcpy(四向真 memmove)、halHostRegister(身份映射) |
| `src/inc/vdriver_internal.h` | 内部共享声明(设备状态查询、日志、决策常量) |
| `tests/test_m1_device_memory.cpp` | 符号契约测试,33 项断言 |
| `tests/run_tests.sh` | 统一测试入口(LD_LIBRARY_PATH 前置) |
| `CMakeLists.txt` | 强符号宏重命名机制 + M1 测试接线 |

### 决策口径落点(实测依据)
- `halGetSocVersion` → `"Ascend910B1"`:runtime 直接把字符串交给 `GetChipTypeFromPlatform` 匹配 ini(runtime.cc:655-696);**`Ascend910B1.ini` 已确认存在于安装包**(data/platform_config/,初判缺失系 find 截断)
- `halGetDeviceInfo(SYSTEM, ADDR_MODE/RUN_MACH)` = UNIFIED/VIRTUAL(D4,命中 npu_driver.cc:85-89)
- `MODULE_TYPE_AICORE/INFO_TYPE_CORE_NUM` = 20;`MODULE_TYPE_AICPU` = 0(D7 fail-fast)
- `drvGetPlatformInfo` = `RT_RUN_MODE_ONLINE`(=1;0 是 OFFLINE 会误触 StreamSyncMode ini 读取,runtime.cc:978/1043)
- 新事实:仓源码头文件与 9.1.0 安装包头**逐字节一致**(md5 比对),常量无版本偏差

### 验收结果(ctest 2/2 通过;M1 断言 33/33)
关键项:RUN_MACH=VIRTUAL、ADDR_MODE=UNIFIED、SoC 名、AICPU=0、内存 512B 对齐、H2D/D2H/D2D 真拷贝回读一致、count>dst_size 拒绝、非 vdriver 指针 Free 拒绝、HostRegister 身份映射。

### 关键发现(沉淀)
- **搜索序实证:LD_LIBRARY_PATH > RUNPATH**——直接运行测试二进制(不加前置)会命中真包 devlib 的 HAL(缺内部符号而炸);统一用 `tests/run_tests.sh`(前置 vdriver)。
- `dvError_t`/`DV_OFFLINE` 是笔误陷阱:`DVresult` 实为 `typedef drvError_t`,`DV_OFFLINE` 为空宏。
- C11 无 `nullptr`(C 文件用 NULL);OBJECT 库在 CMake 3.22 不支持 POST_BUILD custom command。

### Code Review 结论(M1,代理评审 93d80750)
**结论:可合入。** 严重 2 项、建议 6 项、风格 3 项,全部处置:
- 严重①(halMemFree 魔数探测 UB + double-free)——**已修**:魔数头部方案整体废弃,改为**带锁开地址注册表**(精确匹配用户指针,`vdriver_mem_query` 按址反查,同时满足 M2 SQE 代执行需要);
- 严重②(aligned_alloc 尺寸回绕)——**已修**:入口 `size > SIZE_MAX-2*ALIGN` 拒绝;
- 建议⑤(SoC 默认值)——**前提有误已澄清**:`Ascend910B1.ini` 在安装包实际存在(此前 find 截断只见 B2/B/B4);仍采纳"getenv 首读缓存"防中途漂移;
- 建议⑥(vdriver_device_is_open 死代码)——**已启用**:sqcq 的 Allocate/TaskSend 均校验设备打开态;
- 建议⑨(符号导出泄漏)——**已修**:`src/vdriver.map` 链接期白名单(hal*/drv*/vdriver*/sqe_interp* 导出,其余 local);注意 DLLEXPORT 在 Linux 为空宏,不能走 -fvisibility=hidden;
- 其余( strcpy→snprintf、ATOMIC_VAR_INIT、CXX REQUIRED、强符号清单注释)——已采纳。
- 评审实证:nm 13 强符号全 T、ctest 33/33、枚举与 dev.h/ONLINE 逐项核对。

---

## M2 任务通路 ✅(2025-09-01,review 待派)

### 交付物
| 文件 | 说明 |
|---|---|
| `src/sqcq.c` | halSqCqAllocate/Free/Query/Config、halSqTaskSend(解释执行+head=tail 推进+pos 出参)、halCqReportIrqWait(bitmap=0)/Get(count=0)/Release |
| `src/sqe_interp.cpp` | SQE 解释器:SDMA 内联(len@28/src@32/dst@40)、SDMA 间接(base@16/@20high17→rtDavidMemcpyAddrInfo src@32/dst@40/len@48)、PLACE_HOLDER(task_type=90:src@16/dest@24/size@32)、WRITE_VALUE(addr@16/@20,awsize≥3 写 8B)、FFTS/AICPU 计数忽略 |
| `src/inc/sqe_layout.hpp` | 自包含 packed SQE 布局(static_assert 锁定 64B),测试与解释器共用 |
| `src/vdriver.map` | 链接期导出白名单 |
| `tests/test_m2_sqcq.cpp` | M2 验收 22 项断言 |

### 验收结果(ctest 3/3 通过;M2 断言 22/22)
关键项:未开设备拒绝、pos 出参、SDMA 内联 H2D/D2D 链式拷贝内容一致、Ptr 间接模式、PH task_type=90 拷贝、WRITE_VALUE 8B 落地、SQ_HEAD/TAIL=5、STATUS 非 0、CQE_STATUS=0、Config 回拉 head、报告三件套、未知 sqId 拒绝、释放后重分配。

### 关键发现(沉淀)
- **SQE 字段偏移必须逐字节对照真定义,不能按相邻结构推断**:ptrMode 位在字节 15(不是 13);Ptr 模式 base 在 @16/@20(high 仅 17 位,不是 @32);rtDavidMemcpyAddrInfo 是 64B(len@48)。static_assert 首轮就抓出 PhSqe 70B(误用 SqeHeader,PH SQE 是独立布局)与 DavidMemcpyAddrInfo 48B 两个错误。
- **9.1.0 的 memset 不需要解释**:DevMemSetAsync(memcpy_starsv2.cc:246)在 runtime 侧把大块 memset 分解为 host memset + 分块 memcpy SQE——解释器只覆盖 memcpy/write_value 类即可。
- PLACE_HOLDER SQE 是独立布局(task_type@2,payload@16),不与通用 header 共用 block_dim 位。
- 版本脚本注释里同样不能出现 `hal*/drv*` 字样(`*/` 截断注释,与 M0 的 C 注释同一坑)。

### Code Review 结论
(M2 评审代理结论回填处)

---

## M3 opbase 联调(占位)

(待实施:真包 libopapi 跑 aclnnAdd 两段式,L2 验收。)
